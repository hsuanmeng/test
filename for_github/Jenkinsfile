pipeline{
    agent {
    node('testingEnv') {
        customWorkspace '/data/workspace/test_pipeline'}
        }

    stages {
        stage('First'){ steps{ echo 'step start' }}
        stage('Second'){steps{ sh 'make'}}
    }
}